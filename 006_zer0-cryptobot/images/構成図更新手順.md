# 構成図（006_architecture）更新手順

draw.ioで `006_architecture_plugin.drawio` を編集してからサイトに反映するまでの手順。
上から順にコピペで実行すればOK（3以降はClaude Codeにそのまま依頼してもよい）。

## 1. draw.ioアプリで編集・保存

`006_Zer0_CryptoBot/images/006_architecture_plugin.drawio` をdraw.ioデスクトップアプリで開いて編集 → 保存（Ctrl+S）。

## 2. SVGエクスポート（draw.ioアプリ内で操作・手動）

draw.ioアプリのメニューから `ファイル → エクスポート → SVG...` を実行し、
**同じimagesフォルダに `006_architecture_plugin.drawio.svg` という名前で上書き保存**する。
（このファイルが無いとこの後のスクリプトは失敗する）

## 3. 通信アニメーション付きSVGを生成

Windowsのcmdからそのまま貼り付けでOK（`wsl`経由でWSL側のpython3が動く。標準ライブラリのみ使用なので追加インストール不要）。

```
wsl python3 /root/Zer0/006_Zer0_CryptoBot/images/build_architecture_flowdot.py
```

`006_architecture_plugin_flowdot.svg` が再生成される。

## 4. ポートフォリオサイトへ反映

WSL/Linux側（Claude Codeのbash等）で実行。

```bash
cp /root/Zer0/006_Zer0_CryptoBot/images/006_architecture_plugin_flowdot.svg \
   /root/Zer0/004_portfolio/src/public/images/006_architecture.svg
cd /root/Zer0/004_portfolio && bash scripts/deploy.sh
```

## 5. デプロイ確認（本番とローカルのMD5一致チェック）

```bash
curl -s https://www.zer0-infra.com/images/006_architecture.svg | md5sum
md5sum /root/Zer0/004_portfolio/src/public/images/006_architecture.svg
```

## 6. Gitへ同期

```bash
bash /root/Zer0/sync_to_public.sh
```
