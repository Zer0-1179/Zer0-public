# 005_Zenn_Mid_Article_Bot 変更履歴

※ バージョン番号の付与は2026-07-14に廃止（実質的なバージョン管理をしておらず、詳細な変更履歴はGitで追跡可能なため）。過去分の番号（vX.Y）は見出しに参照用として残している。

## 2026-04-20

### 初版リリース（v1）

- Claude Sonnet + 512MB Lambda + 16トピック体制

## 2026-04-28

### 2段階Bedrock呼び出し（v1.1）

- トピック選択にHaikuを使用する2段階呼び出しに変更。月額コスト約30%削減

## 2026-05-05

### 図挿入マーカー方式（v1.2）

- `{DIAGRAM_N}` マーカー方式を導入。Bedrockが図挿入位置と説明文を記事本文内に自然配置するよう改善

## 2026-05-10

### 公式ドキュメント取得（v1.3）

- AWS公式ドキュメント自動取得（primary_serviceのdocs.aws.amazon.com・最大6,000文字）を根拠情報として付与

## 2026-05-20

### テスト実行手段の追加（v1.4）

- Lambda Function URL（AWS_IAM認証）を追加。EventBridgeスケジュールを止めずに任意タイミングでテスト実行可能に

## 2026-06-01

### 検証機能追加・バグ修正5件（v1.5）

- CFn `validate_template` 検証機能を追加
- バグ修正5件（重複トピック・TemplateBody上限・HTMLエスケープ・CFn例外取得・マーカー自動除去）
- 処理時間計測・Bedrockコスト概算ログを追加

## 2026-06-15

### コードレビュー反映（v1.6）

- /tmp 記事フォルダの自動削除（最新5件保持）を002と同様に追加しウォームスタート時のディスク逼迫を防止
- トピック選択の部分一致フォールバックを追加
- Bedrockパラメータ/単価/サイズ上限のマジックナンバーを定数化

### バグ修正2件（v1.7）

- PNG未生成時のDIAGRAMマーカー除去正規表現を `\{\{?DIAGRAM_\d+\}\}?` に修正（`str.format()` 後の単一波括弧も除去対象に）
- 見出しなし記事で `_embed_image_placeholders` が画像を無言スキップする問題を修正（末尾追記＋WARNING ログに変更）

## 2026-06-16

### 記事の構築手順をCLI中心に変更（v1.8）

- 記事内構築手順をCFnテンプレート→AWS CLI中心に変更
- プロンプト改修（変数定義セクション必須・コメント形式・クリーンアップセクション追加等）
- CFn `validate_template` を廃止しCLI必須要素チェックに置換。`cfn` boto3クライアントを削除

### コードレビュー指摘6件修正（v1.9）

- bashブロック例の`\`が行継続として除去されるバグを修正
- 例外ハンドラのCFn表記を修正
- CLIバリデーターをbashブロック内限定awsチェック＋クリーンアップ同義語対応に改善
- 両CFnスタックから`cloudformation:ValidateTemplate` IAM権限を削除

## 2026-06-27

### IAM最小権限化（v2.0）

- 未使用の`ses:SendRawEmail`・`aws-marketplace:*`権限を削除
- Bedrockの`inference-profile/*`ワイルドカードをHaiku明示ARNに置き換え。CFn再デプロイ済み

## 2026-07-01

### Opusコードレビュー反映（v2.1）

- Opus 4.8コードレビュー（10件）を反映
- diagram_generator.pyのauto-padding座標破壊・曲線矢印・垂直間隔・figureリーク等のバグを修正
- `_load_icon()`にlru_cacheを追加。孤立DIAGRAMマーカーのクリーンアップも追加

## 2026-07-02

### 記事精査による実バグ修正（v2.2）

- Opus 4.8でlog_analytics記事を精査
- 構成図が本文のservicesリストと食い違う問題を修正
- CloudTrail経由データのgzipエンベロープ未展開・Athenaパーティション未登録という実バグ2件を発見・修正

### AI技術レビューの自動化（v2.3）

- 記事生成後のAI技術レビュー自動化を追加。Bedrockに構造化出力で技術的正確性を検証させ、指摘をメールに掲載（自動修正なし）
- Haiku推論プロファイルのリージョン起因IAM不足バグも副次的に発見・修正

### レビュー機能の撤回・構成図刷新（v2.4）

- v2.3のレビュー・自動修正機能を撤回（1回書き直し方式の自動修正が新たな矛盾を生む実例を確認）
- 代わりに全16トピックのサービス数を5→3に削減し、構成図生成をservicesリストから自動生成する汎用方式に刷新

### コード監査反映（v2.5）

- security_hardening の primary_service 誤り（waf→guardduty）を修正
- 未使用の secondary_service フィールドと IAM s3:DeleteObject を削除
- 構成図がサービス数4以上でxlim外にクリップされるバグを修正。Lambda Runtime記述をpython3.14に統一

### 構成図の1枚統合（v2.6）

- 構成図をほぼ同一内容の2枚から1枚に統合
- 記事プロンプトの{DIAGRAM_2}マーカー・設計上の考慮ポイント節の図指示を削除し、コスト・セキュリティ・スケーラビリティを図なしで直接解説する構成に変更

### 記事品質改善2点（v2.7）

- primary_serviceの公式ドキュメントURLをコード側で「参考」節に確定挿入（LLMのURL生成なし）
- 「はじめに」に「この記事でわかること」3行サマリーを追加

### Fableコードレビュー反映（v2.8）

- 構成図の矢印を方向性主張なしの直線に変更（実際と逆向きの表示を修正）
- Sonnet5切替時のKeyError修正・S3失敗時のBedrockコスト重複回避・microservices_base等の細部修正

### Fableレビュー続き（v2.9）

- 壊れていたテスト2件を修正しHIGHバグを検出
- `generate_article`のtextブロック抽出がthinking打ち切り時にStopIterationでクラッシュする問題を修正
- BedrockクライアントのReadTimeout自動リトライを無効化（コスト二重発生防止）
- CFnのBedrock ARNリージョンワイルドカードを明示化。回帰テスト追加

## 2026-07-06

### Fableブラッシュアップ（v3.0）

- トピック重複除外を12件に拡張し、再登場時はタイトルに周回数を付与
- 記事品質の機械チェックを追加(検出のみ)。メールに見出しアウトライン・レビューチェックリストを追加
- 根拠ドキュメントを3サービス全件取得。MD+PNGをメール添付。14トピックで構成図矢印を安全に復活

### デプロイ同梱漏れの重大バグ修正（v3.1）

- deploy.shがaws_icons/・fonts/をLambda ZIPに同梱しておらず、本番の構成図は日本語が文字化けしアイコンも自作の色付きボックスに全滅していた重大な既存バグを発見・修正
- 両ディレクトリをzip対象に追加し実機確認済み

## 2026-07-15

### 参考リンクの全サービス対応（v3.2）

- log_analyticsトピックの生成記事をレビューした際、記事末尾「参考」セクションでprimary_service（CloudTrail）のみリンク化され、S3・AthenaはURLなしのプレーンテキストのままになっている問題を発見
- `_inject_reference_link`をprimary_service 1件のみからトピックの services 全件に対応するよう修正。DOCS_URL_MAP・_SERVICE_NAME_TO_DOCS_IDは既に全サービス分のURLを保持していたため、参照範囲を広げるだけで対応可能だった
- プロンプト指示を変更し、LLMは「## 参考」見出しのみ書き本文（リンク一覧）は一切書かないよう変更。全リンクはコード側の確定済みURLから挿入する方式に統一（ハルシネーション防止）
- ユニットテスト2件（全サービスリンク化・対応表に無いサービスのスキップ）を追加し全9件パス。本番Lambdaへデプロイ・test_mode実行（マルチリージョンDR構成トピック）でRoute 53・RDS・S3 Cross-Region Replicationの3件全てが正しいURLでリンク化されることを実機確認済み

### トピック選択の偏り修正・test_mode分離（v3.3）

- ユーザーから「最近の記事の質が似ている」「使用サービスが同じことが多い」との指摘を受け、Fableモデルにコード・過去記事・SSM履歴の調査を依頼
- SSMパラメータ `/mid-article-bot/recent-topics` の全バージョン履歴（v1〜v43）を全数集計した結果、`select_topic_with_bedrock`がBedrockに「ランダムに選んでください」と依頼していたトピック選択が実際には強く偏っており、log_analyticsが全41回の選択中9回（22%）選ばれる一方、serverless_ec・static_web_hosting・container_platform・backup_dr・security_hardingの5トピックは一度も選ばれていなかったことが判明
- さらに、開発中のtest_mode実行（動作確認・デバッグ用の手動起動）が本番と同じSSMパラメータを無条件に更新し続けており、本番のトピック重複除外（直近12件を除外する仕組み）が実質機能していなかったことも判明。SSM履歴を辿ると、05-01と07-15のlog_analytics再登場は、いずれも直前の集中的なtest実行によって直近の本番選択分がFIFO除外リストから押し出されたことが直接の原因だった
- `select_topic_with_bedrock`を廃止し、コード側の`random.choice`で選ぶ`select_topic`に置き換え。Bedrock呼び出し自体が不要になり偏りが解消（副次的にコスト・レイテンシも削減）
- test_modeはSSMトピック履歴（除外リスト・周回カウント）を一切更新しないよう変更。S3保存先も本番の`zenn-mid-articles/`から`zenn-mid-articles-test/`に分離し、メール件名にも`【TEST】`を付与
- `save_topic_to_ssm`が保存直前にSSMを再取得する実装になっており、一時的な読み込みエラー時に履歴が`[topic_id]`1件だけに縮退するリスクがあったため、Step 1で取得済みのリストをそのまま渡す方式に修正
- ローカル実行（`python3 lambda_function.py`）がデフォルトでtest_modeにならず、Sonnet本番課金・本番SSM/S3更新をしてしまうバグも合わせて修正（デフォルトtest_mode化。`LOCAL_TEST_MODE=0`で本番相当実行も可能）
- 汚染されていたSSM状態（トピック除外リスト・周回カウント）を、ローカルoutput/フォルダの実生成タイムスタンプ（毎月1日・15日21:00 JST採取分のみ）から再構成した実績値に手動復旧
- ユニットテスト3件（偏りのない除外・全除外時のリセット・SSM再取得しないことの検証）を追加し全12件パス。本番Lambdaへコードデプロイ済み。記事生成を伴う実行はBedrockコストとSSM/S3状態の汚染を避けるため今回は実施せず、ユニットテストとコード検証のみで動作を確認

## 2026-07-16

### トピックプール拡充 16→24種類（v3.4）

- v3.3でトピック選択の偏りバグは修正したが、そもそも16トピックが固定3サービスの組み合わせで構成されているため、S3が16トピック中7回・Lambdaが5回登場するなど、選択が均一になっても「サービス自体の偏り」は残る構造的な問題が残っていた
- 既存の16トピックで使われていないAWS公式アイコン（`aws_icons/`に既に同梱済みの65種のうち未使用のもの）を軸に、S3・Lambdaを一切含まない新規8トピックを追加: `vpc_network`（VPC+NAT Gateway+Internet Gateway）、`async_orchestration`（EventBridge+Step Functions+SQS）、`cicd_ec2_deploy`（Auto Scaling+CodeDeploy+EC2）、`user_auth_cognito`（Cognito+API Gateway+IAM Role）、`observability_monitoring`（CloudWatch+CloudWatch Alarm+SNS）、`edge_security`（CloudFront+WAF+Shield）、`secrets_rotation`（Secrets Manager+EC2+IAM Role）、`data_governance`（Lake Formation+Glue+IAM）
- 新サービス分の`DOCS_URL_MAP`・`_SERVICE_NAME_TO_DOCS_ID`エントリを追加。URLは全12件をWebFetchで実際にアクセスして実在確認してから登録（既存の「LLMにURLを生成させずコード側で確定挿入する」ハルシネーション防止方針を、新規追加時の運用にも適用）
- 調査の過程でAmazon QuickSightが2025年10月〜2026年にかけて「Amazon Quick」へ改称・機能再編中であることが判明。名称がまだ安定していないため新トピックへの採用は見送り、代わりにプロンプトの「AWSサービス名の最新化」テーブルに改称情報を追記し、既存トピックの記事内で誤って古い名称が使われることを防止
- `diagram_generator.py`の`_SERVICE_ICON_KEYWORDS`に新サービス分のアイコンキーワードを追加。「IAM Role」を汎用の「IAM」より先に判定させ、部分一致で汎用アイコンに吸収されないよう順序を調整
- 整合性チェックスクリプトで全24トピック×全サービスが`DOCS_URL_MAP`・アイコンファイル・`_ZENN_META`と過不足なく対応していることを確認。新8トピックはローカルで実際に構成図をレンダリングし、アイコン解決（人物アイコンへのフォールバックが発生していないか）・日本語表示・レイアウト崩れがないことを目視確認
- 本番Lambdaへコードデプロイ済み。記事生成を伴う実機テストは前回（v3.3）に続き今回も見送り、上記の整合性チェックとローカル図生成のみで動作を確認

### トピック内サービス組み合わせの変動枠＋書き出しバリエーション追加（v3.5）

- v3.4でトピックプール自体は24種類に拡充したが、各トピックの3サービス組み合わせは依然として完全固定だったため、Fable（`model: "fable"`）に調査だけでなく実装まで依頼し、2点を改善した
- **サービス組み合わせの変動枠**: `serverless_ec`（DynamoDB⇔Aurora Serverless v2）・`static_web_hosting`（ACM⇔Route 53）・`cicd_pipeline`（ECR⇔CodeDeploy⇔S3の3択）・`log_analytics`（S3⇔AWS Glue⇔Amazon Data Firehoseの3択）・`backup_dr`（DynamoDB⇔EC2）・`observability_monitoring`（CloudWatch Alarm⇔EventBridge）の6トピックに`variants`（差分辞書のリスト）を追加し、`select_topic()`が基本形とバリエーションから`random.choice`で1つに確定してから返す方式にした（呼び出し側の`generate_article`・`diagram_generator`・`fetch_aws_docs`は無変更で動作）
- 新規サービス名（Amazon Data Firehose・Aurora Serverless v2）の公式ドキュメントURLをWebFetchで実在確認の上`DOCS_URL_MAP`に追加し、`diagram_generator.py`の`_OFFICIAL_ICON_MAP`にも公式アイコン（firehose・aurora）を追加。Lambda同梱用に`src/aws_icons/`へ2ファイルコピー
- `_TOPIC_FLOWS`（構成図の矢印方向定義）を「トピックID→flow定義のリスト」に構造変更し、現在選ばれているサービス組み合わせと一致するflowがある場合のみ有向矢印を描画。log_analyticsのGlue/Firehose変種はCloudTrailとの間にS3やCloudWatch Logsを挟む間接連携のため、誤った矢印を描くより安全な無向線にフォールバックさせている
- **記事の書き出しバリエーション**: `## はじめに`の冒頭例文がほぼ毎回「失敗談引用型」に偏っていた（7記事中6記事）ため、プロンプト内の完全な例文を廃止し、5パターンの書き出しアプローチ（失敗・障害／具体的数字／読者への問いかけ／ビフォーアフター／典型シナリオ）からコード側`random.choice`で1つだけを指示として埋め込む方式に変更（LLMに「ランダムに選ばせる」とv3.3と同じ偏りが再発するため、乱択は必ずコード側で行う）
- 構成図直前の予告文の「良い例」も同じ理由で逐語転写が確認されていたため削除し、3パターンの切り口指示の乱択に変更。`## 各コンポーネントの選定理由`と`## 構成手順`の出現順序もコード側乱択で入れ替え、見出し構成の紋切り型化を緩和
- ユニットテスト7件を新規追加し全19件パス。6トピック×全バリエーション計32通りをローカルで構成図レンダリングし、人物アイコンへのフォールバックなし・矢印のノード貫通なしを確認。プロンプト整形も200回ランダム試行で`{{DIAGRAM_1}}`マーカー破損やプレースホルダー残留がないことを確認
- 本番Lambdaへコードデプロイ済み。記事生成を伴う実機テストは前回・前々回に続き今回も見送り、ユニットテスト・ローカル図生成・プロンプト整形検証のみで動作を確認
- 後日、ユーザー確認のためtest_mode実機実行を実施したところ、v3.3（2026-07-15）でtest_mode用S3保存先を`zenn-mid-articles/`から`zenn-mid-articles-test/`に分離した際、CFnのIAMポリシー（`s3:PutObject`のResource）を追従し忘れていたバグを発見（AccessDeniedでアップロード失敗、Lambda自体は例外を握りつぶし200 OKで完了するため気付きにくい設計だった）。v3.3・v3.4は実機テストを見送っていたため2回のセッションで発覚しなかった。IAMポリシーに`zenn-mid-articles-test/*`のResourceを追加してCFnスタックを更新し、再実行でS3アップロード成功・エラーゼロを確認。テスト成果物はS3から削除済み

## 2026-08-01

### メール本文の`download_article.sh`パス不整合を修正

- 2026-07-14の`scripts/`フォルダ移行後もメール本文（テキスト・HTML両方）の案内コマンドが旧パス`bash ~/Zer0/005_Zenn_Mid_Article_Bot/download_article.sh`のまま残っており、案内通りに実行するとファイルが見つからず失敗する状態だった
- `bash ~/Zer0/005_Zenn_Mid_Article_Bot/scripts/download_article.sh`に修正し、実ファイルの配置と一致させた

### ローカル`output/`の自動クリーンアップを廃止

- ユーザーから「ローカルの記事を削除すると、いつ投稿したのか分からなくなる」と指摘を受け、`_cleanup_old_articles()`（最新5件超過分を自動削除する仕組み）を廃止した
- `OUTPUT_KEEP_MAX`環境変数・`_cleanup_old_articles()`関数・呼び出し箇所を削除した
- ローカルの`output/`配下は投稿履歴として無期限に蓄積される方針に変更

## 2026-08-02

### 記事品質ガードレール強化

- 2026-08-01の実行事故（dry_run未実装のまま呼び出し、本番相当実行8回・実課金$1.42・実メール8通が発生）で生成された8記事（009〜016）をFableで個別レビューしたところ、全8記事でHIGH級（実行不能なコマンド・本文と実装/構成図の矛盾・コスト試算の重大な誤り等）の指摘が合計30件以上見つかった
- ユーザーから「記事の質が思ったより悪いな」という直接のフィードバックを受け、002が2026-07-16に実施したのと同様の「重大バグパターンを発見→プロンプトへガードレール追加」のサイクルを005にも適用した
- `ARTICLE_PROMPT_TEMPLATE`（`src/lambda_function.py`）へ4点のガードレールを追加:
  - **自己整合性チェック**: 本文中で「実現する」「作成する」「自動化する」と明言した機能は、対応するCLIコマンド・コード・設定を本文中に必ず含める指示を追加（012・014の「自動ローテーションを実現する」と謳いながら実装がない不一致、013の「タスクロールを作る」と実装の乖離を踏まえた対策）
  - **AWS CLI shorthand構文の禁止**: `--attributes`/`--policy`/`--message-attributes`等JSONを受け取るオプションに`Key=$(cat file.json)`のようなコマンド置換でJSONを直接埋め込まない指示を追加（009・015等で複数回発生した実際にAWS側でパースエラーになる誤りパターン）
  - **コスト試算の検算**: 単価×数量の計算式明記・合計欄との整合性確認・日額/月額混同防止・無料枠控除の有無明示を追加（8記事中5記事で日額/月額混同や桁ずれが発生していた）
  - **構成図と本文の整合性**: 構成図は`services`リストから機械的に生成されるため、本文の説明（データ・リクエストの流れ）はサービス一覧に含まれる範囲・関係性と一致させ、記載のない分岐（ファンアウト・並列処理等）を書かない指示を追加（009・010・015で図にない挙動を本文が主張する不一致が発生していた）
- 全24トピック×3回（計72通り）でプロンプト整形にエラーがないことをローカルで確認。既存ユニットテスト19件（`build_article_prompt`のカバレッジ含む）全件パス
- `dry_run: true`+`test_mode: true`（Haiku使用）で実機実行し、新プロンプトでの記事生成（25,424文字、S3/SSM/メール送信は全てスキップ）が正常に完了することを確認。実課金・実メール送信は発生させていない
- 本番Lambdaへコードデプロイ済み（プロンプト変更のみでCFnパラメータの変更はないため、`aws lambda update-function-code`による直接デプロイのみ実施、CloudFormationスタックの更新は不要と判断）
