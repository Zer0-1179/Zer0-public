AWSTemplateFormatVersion: "2010-09-09"
Description: "CloudWatch Alarms for EFS - I/O limit with WARNING/CRITICAL severity levels"

# -----------------------------------------------
# DEPLOY
# -----------------------------------------------
# aws cloudformation deploy \
#   --stack-name <ProjectName>-<Env>-alarm-efs \
#   --template-file cfn-cw-alarm-efs.yaml \
#   --parameter-overrides \
#     ProjectName=my-project \
#     Env=prd \
#     FileSystemId=fs-0123456789abcdef0
#
# With SNS notification (WARNING → Slack, CRITICAL → PagerDuty):
# aws cloudformation deploy \
#   --stack-name <ProjectName>-<Env>-alarm-efs \
#   --template-file cfn-cw-alarm-efs.yaml \
#   --parameter-overrides \
#     ProjectName=my-project \
#     Env=prd \
#     FileSystemId=fs-0123456789abcdef0 \
#     WarningActionArn=$SLACK_SNS_ARN \
#     CriticalActionArn=$PAGERDUTY_SNS_ARN
#
# PercentIOLimit applies to General Purpose performance mode only.
# For Elastic Throughput, this metric is not reported (alarm stays INSUFFICIENT_DATA).
# -----------------------------------------------

# -----------------------------------------------
# Metadata
# Organizes the parameter input UI in the CloudFormation console.
# ParameterGroups groups parameters visually.
# -----------------------------------------------
Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label:
          default: "General"
        Parameters:
          - ProjectName
          - Env
      - Label:
          default: "Target File System"
        Parameters:
          - FileSystemId
      - Label:
          default: "WARNING Thresholds"
        Parameters:
          - PercentIOLimitWarningThreshold
      - Label:
          default: "CRITICAL Thresholds"
        Parameters:
          - PercentIOLimitCriticalThreshold
      - Label:
          default: "Common"
        Parameters:
          - EvaluationPeriods
          - DatapointsToAlarm
          - ActionsEnabled
      - Label:
          default: "Notifications (optional)"
        Parameters:
          - WarningActionArn
          - CriticalActionArn
          - InsufficientDataActionArn

# -----------------------------------------------
# Parameters
# Values passed in at deploy time.
# AllowedValues restricts accepted input.
# Default sets the fallback value when omitted.
# -----------------------------------------------
Parameters:
  ProjectName:
    Type: String
    Default: my-project
    AllowedPattern: "^[a-z0-9-]+$"
    ConstraintDescription: "Lowercase letters, numbers, and hyphens only."

  Env:
    Type: String
    Default: dev
    AllowedValues:
      - stg
      - dev
      - prd

  FileSystemId:
    Type: String
    Description: "EFS file system ID (e.g. fs-0123456789abcdef0)."
    Default: ""
    AllowedPattern: "^(fs-[0-9a-f]+)?$"
    ConstraintDescription: "Must be a valid EFS file system ID (fs-xxxxxxxx) or empty."

  PercentIOLimitWarningThreshold:
    Type: Number
    Default: 80
    MinValue: 1
    MaxValue: 100
    Description: "I/O limit usage WARNING threshold (%). General Purpose mode only."

  PercentIOLimitCriticalThreshold:
    Type: Number
    Default: 95
    MinValue: 1
    MaxValue: 100
    Description: "I/O limit usage CRITICAL threshold (%). General Purpose mode only."

  EvaluationPeriods:
    Type: Number
    Default: 3
    MinValue: 1
    Description: "Number of consecutive 1-minute periods that must breach the threshold before alarming."

  DatapointsToAlarm:
    Type: Number
    Default: 0
    MinValue: 0
    Description: "M out of N evaluation (M value). 0 = use EvaluationPeriods (all periods must breach). Must be <= EvaluationPeriods."

  ActionsEnabled:
    Type: String
    Default: "true"
    AllowedValues:
      - "true"
      - "false"
    Description: "Set to false to disable all alarm actions (useful during maintenance windows)."

  WarningActionArn:
    Type: String
    Default: ""
    Description: "SNS topic ARN for WARNING severity (e.g. Slack). Used for both ALARM and OK notifications."

  CriticalActionArn:
    Type: String
    Default: ""
    Description: "SNS topic ARN for CRITICAL severity (e.g. PagerDuty). Used for both ALARM and OK notifications."

  InsufficientDataActionArn:
    Type: String
    Default: ""
    Description: "SNS topic ARN for INSUFFICIENT_DATA state. Leave empty to skip."

# -----------------------------------------------
# Conditions
# Boolean flags derived from parameter values.
# Used in Resources and Properties via: !If [ConditionName, IfTrue, IfFalse]
# -----------------------------------------------
Conditions:
  HasWarningAction:           !Not [!Equals [!Ref WarningActionArn, ""]]
  HasCriticalAction:          !Not [!Equals [!Ref CriticalActionArn, ""]]
  HasInsufficientDataAction:  !Not [!Equals [!Ref InsufficientDataActionArn, ""]]
  HasDatapointsToAlarm:       !Not [!Equals [!Ref DatapointsToAlarm, 0]]
  IsActionsEnabled:           !Equals [!Ref ActionsEnabled, "true"]

# -----------------------------------------------
# Resources
# -----------------------------------------------
Resources:

  PercentIOLimitWarningAlarm01:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${ProjectName}-${Env}-efs-${FileSystemId}-io-limit-warning"
      AlarmDescription: !Sub "EFS I/O limit > ${PercentIOLimitWarningThreshold}% for ${EvaluationPeriods} x 1 min. [WARNING] (General Purpose mode only)"
      Namespace: AWS/EFS
      MetricName: PercentIOLimit
      Dimensions:
        - Name: FileSystemId
          Value: !Ref FileSystemId
      Statistic: Average
      Period: 60
      EvaluationPeriods: !Ref EvaluationPeriods
      DatapointsToAlarm: !If [HasDatapointsToAlarm, !Ref DatapointsToAlarm, !Ref AWS::NoValue]
      Threshold: !Ref PercentIOLimitWarningThreshold
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      ActionsEnabled: !If [IsActionsEnabled, true, false]
      AlarmActions:           !If [HasWarningAction,  [!Ref WarningActionArn],  !Ref AWS::NoValue]
      OKActions:              !If [HasWarningAction,  [!Ref WarningActionArn],  !Ref AWS::NoValue]
      InsufficientDataActions: !If [HasInsufficientDataAction, [!Ref InsufficientDataActionArn], !Ref AWS::NoValue]

  PercentIOLimitCriticalAlarm01:
    Type: AWS::CloudWatch::Alarm
    Properties:
      AlarmName: !Sub "${ProjectName}-${Env}-efs-${FileSystemId}-io-limit-critical"
      AlarmDescription: !Sub "EFS I/O limit > ${PercentIOLimitCriticalThreshold}% for ${EvaluationPeriods} x 1 min. [CRITICAL] (General Purpose mode only)"
      Namespace: AWS/EFS
      MetricName: PercentIOLimit
      Dimensions:
        - Name: FileSystemId
          Value: !Ref FileSystemId
      Statistic: Average
      Period: 60
      EvaluationPeriods: !Ref EvaluationPeriods
      DatapointsToAlarm: !If [HasDatapointsToAlarm, !Ref DatapointsToAlarm, !Ref AWS::NoValue]
      Threshold: !Ref PercentIOLimitCriticalThreshold
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: notBreaching
      ActionsEnabled: !If [IsActionsEnabled, true, false]
      AlarmActions:           !If [HasCriticalAction, [!Ref CriticalActionArn], !Ref AWS::NoValue]
      OKActions:              !If [HasCriticalAction, [!Ref CriticalActionArn], !Ref AWS::NoValue]
      InsufficientDataActions: !If [HasInsufficientDataAction, [!Ref InsufficientDataActionArn], !Ref AWS::NoValue]

# -----------------------------------------------
# Outputs
# Values available after stack creation.
# Export allows other stacks to reference via !ImportValue.
# -----------------------------------------------
Outputs:

  PercentIOLimitWarningAlarmArn01:
    Description: "I/O limit WARNING alarm ARN"
    Value: !GetAtt PercentIOLimitWarningAlarm01.Arn
    Export:
      Name: !Sub "${AWS::StackName}-PercentIOLimitWarningAlarmArn"

  PercentIOLimitCriticalAlarmArn01:
    Description: "I/O limit CRITICAL alarm ARN"
    Value: !GetAtt PercentIOLimitCriticalAlarm01.Arn
    Export:
      Name: !Sub "${AWS::StackName}-PercentIOLimitCriticalAlarmArn"
